
      <tr class="<%= row_class %>">
        <th/>
        <% resultset['header'].each do | column | %>
          <th><%= column['table_name'] %></th>
        <% end %>
      </tr>

      <tr class="<%= row_class %>">
        <th/>
        <% resultset['header'].each do | column | %>
          <th><%= column['column_name'] %></th>
        <% end %>
      </tr>

      <tr class="<%= row_class %>">
        <th/>
        <% resultset['header'].each do | column | %>
          <th><%= column['data_type'] %></th>
        <% end %>
      </tr>
