<% databases.each do |database|%>
  <% if database.id == id  %>
    <strong><%= database.name %></strong>
  <% else %>
    <%= link_to database.name, send("#{sibling_path}", database) %>
  <% end %>
<% end %>
